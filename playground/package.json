{
  "name": "mylang-playground",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "cd .. && wasm-pack build mylang-playground --release --target web --out-dir ../playground/pkg --out-name mlang_playground",
    "build": "npm run build:wasm && vite build",
    "dev": "npm run build:wasm && vite",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.8.0"
  }
}