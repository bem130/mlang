{
  "name": "mylang-playground",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "cd .. && wasm-pack build mylang-playground --release --target web --out-dir ../playground/pkg --out-name mlang_playground --no-opt",
    "build": "npm run build:wasm && vite build",
    "dev": "vite",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
