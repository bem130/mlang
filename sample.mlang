fn calculation(a: f64) -> f64 {
    // 数式ブロック内での関数呼び出し
    $ a * 2.0 $
}

// main関数の戻り値は常に () となる
fn main() {
    // if式の結果を変数に束縛し、さらに型注釈を付与する
    let result = if true { 10 } else { 20 } : i32;

    // S式 (関数呼び出し)
    calculation(3.0);

    // ブロックの最後に式がないため、このmain関数は暗黙的に () を返す
}