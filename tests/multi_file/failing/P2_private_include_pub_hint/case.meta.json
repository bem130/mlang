{
  "entry": "main.mlang",
  "expect": "error",
  "error": {
    "variant": "compile",
    "message_contains": "`secrets::token` は private です"
  },
  "notes_contains": [
    "namespace の外から利用するには、定義に `pub` を付けてください"
  ]
}
