// ラムダ式を変数に束縛
fn add_one |x: i32|->i32 $x + 1$;
let result add_one(10);
println(i32_to_string(result));

// 'fn' は 'let hoist' のシンタックスシュガー
fn factorial |n: i32|->i32
    if $n == 0$ {
        1
    } else {
        $n * factorial($n - 1$)$
    };
println(i32_to_string(factorial(5)));