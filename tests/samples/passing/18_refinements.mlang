// Refinement types sample: ensure simple SMT-backed refinement passes

// A function that accepts a non-negative integer and returns it incremented
fn incr |x: <x: i32 | $x >= 0$>|->i32
    $x + 1$;

// Call the function with a valid argument
let v incr(5);
println(string_concat("incr(5) = ", i32_to_string(v)));